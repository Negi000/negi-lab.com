This directory is reserved for WebAssembly assets used by browser tools.

Keep WASM binaries and their loader notes here when a tool needs client-side
native-speed processing. The current public tools do not require a WASM bundle.
